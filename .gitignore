# Vim plugins live in .vim/pack/plugins/start/ as git submodules, so they are
# tracked as pinned pointers -- nothing to ignore here.

# Vim artifacts
.vim/pack/*/start/*/doc/tags
.netrwhist
*.swp
*.swo

.DS_Store
